import requests
import json
import os
import time
import httpx
import base64
import tkinter
import ctypes
import jsonify
from tkinter import * # import everything 

tokens = ["", "", "", "", "", "", ""]
guild_id = int_amount_wip
api_url = f""

def on_resize(event):
    print("window resized to {}x{}".format(event.width, event.height))

def ui:
    root = Tk()
    root.geometry()